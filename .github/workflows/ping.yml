name: Keep Alive Ping

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Isidora Veri Sugc
        run: |
          curl -s https://www.isidoraverisugc.com/
          echo "Pinged at $(date)"
